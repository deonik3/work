#include <iostream>
#include <matrix.hpp>
int main(){
  size_t M = 0, N = 0;
  int ** matrix = createMatrix(M,N)
  std::cin >> M >> N;
  std:;cout << M << "" << N << "\n";
  deleteTable(matrix, M, N);
}

